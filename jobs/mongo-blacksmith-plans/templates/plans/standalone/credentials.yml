---
credentials:
  mongo:
    url:      (( concat "mongodb://" meta.username ":" meta.password "@" jobs.mongod/0.ips[0] ))
    host:     (( grab jobs.mongod/0.ips[0] ))
    username: (( grab meta.username ))
    password: (( grab meta.password ))
